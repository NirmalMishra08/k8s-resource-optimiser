apiVersion: apps/v1
kind: Deployment
metadata:
  name: optimizer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: optimizer
  template:
    metadata:
      labels:
        app: optimizer
    spec:
      containers:
      - name: optimizer
        image: optimizer:latest
        ports:
        - containerPort: 8080